Done. Summary of fixes:

**Stats layout, rebuilt to the exact spec:**
1. Ability Scores (unchanged)
2. Challenge, AC, Current HP, Max HP — 2-wide
3. Hit Dice, Save DC, Passive Perception, Speed — 3-wide (fills the row exactly)
4. Size, Alignment, Languages — 2-wide
5. Resistances, Immunities, Vulnerabilities — 2-wide (shrunk from the old wide boxes)
6. Senses, Saving Throws, Skills — 2-wide
7. Spells — its own full-width row (genuinely irregular/long content, doesn't fit a 2-wide box)

**Creature Type removed from Stats** — it's redundant with Identity's own Creature Type select now. Migrated Deep One Priest's `stats.type: "Humanoid"` directly into `record.type: "humanoid"` so the data isn't orphaned, and its Identity select will now show it correctly instead of blank.

**Signature Feature removed from Identity** — it's already shown via the "Signature" badge on the Features list row.

I kept this scoped to exactly what you asked — didn't extend the Type-matching into the shared import converter for future monsters, since that wasn't part of this request.